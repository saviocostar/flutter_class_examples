import 'package:camp_examples/conversor_moedas/currency_app.dart';
import 'package:flutter/material.dart';

main() {
  runApp(const CurrencyApp());

  // Para executar alguns exemplos visto em aula, substituir o
  // CurrencyApp pelo ClassroomExamplesApp()
  // _________________________________________________________
  // runApp(const ClassroomExamplesApp());
}
